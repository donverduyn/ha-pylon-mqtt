# Runtime requirements (mirrors manifest.json)
serialx>=1.5.0

# Home Assistant core — provides all HA types for IDE autocomplete & type checking
homeassistant

# Testing
pytest>=7.0.0
pytest-cov
pytest-asyncio

# Code quality
ruff
