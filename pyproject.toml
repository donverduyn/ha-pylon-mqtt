[tool.pytest.ini_options]
testpaths   = ["tests"]
pythonpath  = ["tests", ".", "docker"]     # "." makes custom_components importable; "docker" exposes pylontech_parser and structs
addopts     = "-v --tb=short --allow-hosts=127.0.0.1,localhost"
asyncio_mode = "auto"
